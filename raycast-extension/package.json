{
  "name": "google-docs-markdown-sync-raycast",
  "title": "GDMS",
  "version": "0.8.5",
  "private": true,
  "description": "Pair the active Google Doc or Sheet with a local workspace",
  "author": "GDMS Contributors",
  "license": "MIT",
  "icon": "icon.png",
  "main": "src/pair-google-doc.tsx",
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "lint": "ray lint",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@raycast/api": "^1.104.5",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "typescript": "^5.9.0"
  },
  "commands": [
    {
      "name": "pair-google-doc",
      "title": "Pair Google Doc or Sheet with GDMS",
      "description": "Pair the active browser's Google Doc with Markdown or Sheet with CSV files",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "workspaceRoot",
      "title": "Local Workspaces Root",
      "description": "Folder containing the local workspaces GDMS should search",
      "type": "directory",
      "required": true
    },
    {
      "name": "serviceRoot",
      "title": "Sync Service Root",
      "description": "Folder containing this project's src/cli.js",
      "type": "directory",
      "required": true
    },
    {
      "name": "oauthClientPath",
      "title": "Google OAuth Client JSON",
      "description": "OAuth desktop client JSON stored outside Git",
      "type": "file",
      "required": true
    },
    {
      "name": "nodePath",
      "title": "Node Executable Override",
      "description": "Optional absolute path to Node.js; leave blank to use Raycast's Node runtime",
      "type": "textfield",
      "required": false
    }
  ]
}
